class Availability {
    constructor(id, dateFrom, dateTo, available, unitId) {
        this.id = id;
        this.dateFrom = dateFrom;
        this.dateTo = dateTo;
        this.available = available;
        this.unitId = unitId
    }
}